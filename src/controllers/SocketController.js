
class SocketController {

  /**
   * This class handles socket connections for the app
   *
   * @param {*} socketInstance
   * 
   * @memberof SocketController
   */
  static onConnet(socketInstance) {
    if (socketInstance) {
      console.log("Socket connected");
    }

    // Code here guys!!
  }
}

export default SocketController;